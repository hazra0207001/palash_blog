
    <table>
      <tr>
        <th>First Name</th>
        <th>Last Name </th>
        <th>Email </th>
        <th></th>
        <th></th>
      </tr>

<% @user.each do |u| %>
 <tr>
   <td><%=u.firstname  %></td>
     <td><%= u.lastname %></td>
     <td><%= u.email %> </td>
   <td><%=link_to "Edit","/users/edit"+"?id="+u.id.to_s %></td>
   <td><%=link_to "Delete","/users/delete"+"?id="+u.id.to_s %></td>
 </tr>
<% end %>
    </table>
<% if flash[:notice] %>
    <p class="flash-notice"><%= flash[:notice] %></p>
<% end %>
