<%= form_for :comment,:url=>"/comments/create" do |f| %>
    <%= f.hidden_field :blog_id, :value=>params[:id]%>
    <%= f.label :comment %>
    <%= f.text_area :blog_comment %>

    <%=f.submit "Submit"%>
<% end %>
